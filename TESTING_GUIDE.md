# 🧪 Локальное тестирование MarketMonitor

**Дата:** 2024-12-04
**Версия:** 0.2.0
**Статус:** ✅ Dev Server Running

---

## ✅ Статус подготовки

```
✅ npm install - 370 пакетов установлено
✅ Vite сервер запущен - http://localhost:3001
✅ Hot Module Reload (HMR) работает
✅ AppLayout импорт исправлен
⚠️ Warning с ?? оператором (не критично)
```

---

## 🚀 Запуск локального сервера

### Способ 1: Если сервер уже запущен
Просто открой в браузере:
```
http://localhost:3001
```

### Способ 2: Если нужно запустить сервер
```bash
cd frontend
npm run dev
```

Вывод:
```
Vite v5.4.21 ready in 471 ms
➜ Local:   http://localhost:3001/
```

---

## 🧪 Что протестировать

### 1️⃣ Маршрутизация

#### ✅ Публичные страницы (доступны без входа)
- [ ] `/login` - открывается форма входа
- [ ] `/register` - открывается форма регистрации

#### ✅ Защищенные страницы (требуют логина)
- [ ] `/` - редирект на `/login` (если не авторизован)
- [ ] `/unauthorized` - показывает ошибку 403 (если попытаться зайти с неподходящей ролью)
- [ ] `/*` - 404 страница для неизвестных маршрутов

### 2️⃣ Страница входа (/login)

#### Проверь элементы:
- [ ] Форма с полями Email и Пароль
- [ ] Кнопка "Войти"
- [ ] Ссылка "Зарегистрироваться" (переход на /register)
- [ ] Gradient фон (фиолетовый)
- [ ] Заголовок "MarketMonitor - Вход в систему" (или подобное)

#### Функциональность:
- [ ] Пустая форма - кнопка заблокирована или показывает ошибку
- [ ] Некорректный email - показывает ошибку валидации
- [ ] Пароль короче 6 символов - показывает ошибку
- [ ] Правильные учетные данные - переход на дашборд
- [ ] Неправильные учетные данные - показывает ошибку "Failed to login"

### 3️⃣ Страница регистрации (/register)

#### Проверь элементы:
- [ ] Форма с полями Email, ФИ (опционально), Пароль, Подтверждение пароля
- [ ] Checkbox "Я согласен с условиями"
- [ ] Кнопка "Зарегистрироваться"
- [ ] Ссылка "Уже есть аккаунт?" (переход на /login)

#### Функциональность валидации:
- [ ] Email обязателен
- [ ] Пароль минимум 8 символов
- [ ] Пароль должен содержать заглавные, строчные буквы и цифры
- [ ] Пароли должны совпадать
- [ ] Условия должны быть согласованы
- [ ] Успешная регистрация - редирект на /login с сообщением

### 4️⃣ Дашборд (/)

Если успешно авторизовался:

#### Проверь элементы:
- [ ] Заголовок "Добро пожаловать, [ИМЯ]"
- [ ] 3 карточки статистики:
  - [ ] "Всего событий" (число)
  - [ ] "Обработано сегодня" (число)
  - [ ] "Активные расписания" (число)
- [ ] Раздел "Последние события" (пока пусто - это нормально)

#### Навигация:
- [ ] Сайдбар слева (на десктопе)
- [ ] Пункты меню: Дашборд, События, Отчеты
- [ ] Для админов: пункт "Администрирование" с подменю
- [ ] Профиль пользователя (верхний правый угол)
- [ ] Кнопка выхода

### 5️⃣ AppLayout (навигация)

#### На десктопе:
- [ ] Сайдбар слева с меню
- [ ] Шапка вверху с логотипом и профилем
- [ ] Кнопка сворачивания сайдбара

#### На мобильном:
- [ ] Сайдбар скрыт
- [ ] Кнопка меню в шапке (открывает drawer)
- [ ] Drawer меню выдвигается слева

#### Профиль:
- [ ] Клик на профиль - выпадающее меню
- [ ] Пункт "Профиль" (заглушка)
- [ ] Пункт "Выход" (красная кнопка)

### 6️⃣ Ant Design компоненты

- [ ] Все компоненты корректно стилизованы
- [ ] Иконки отображаются правильно
- [ ] Кнопки имеют hover эффекты
- [ ] Форма элементов красивая
- [ ] Модальные окна (если будут) отображаются правильно

---

## 🔍 Отладка

### Проблемы при запуске

#### Ошибка: "Port 3000 is in use"
✅ Нормально! Сервер переходит на 3001. Открой:
```
http://localhost:3001
```

#### Ошибка: "Failed to resolve import"
❌ Проблема с путем импорта. Проверь:
1. Файл существует в правильной папке
2. Путь правильный (с правильным количеством `../`)
3. Сохрани файл - HMR перезагрузит

#### Белый экран
🔧 Открой Developer Tools (F12):
1. Вкладка Console - есть ли ошибки?
2. Вкладка Network - загружаются ли ресурсы?
3. Проверь в Application -> Local Storage есть ли данные Supabase?

#### TypeScript ошибки
📘 TypeScript strict mode может показывать ошибки. Это нормально для разработки. Проверь:
1. Type definitions в `src/shared/types/index.ts`
2. Правильно ли типизирован компонент?

---

## 🚀 Тестирование с Supabase

### Предусловие
Нужно иметь заполненный `.env.local` с:
```
VITE_SUPABASE_URL=https://xxxxx.supabase.co
VITE_SUPABASE_ANON_KEY=xxxxxxxxxxxxxx
```

### Тестирование регистрации
1. Откройся на `/register`
2. Введи новый email (например, `test@example.com`)
3. Введи пароль (минимум 8 символов, с заглавными, строчными и цифрами)
4. Подтверди пароль
5. Согласись с условиями
6. Кликни "Зарегистрироваться"
7. Должен быть редирект на `/login` с сообщением "Регистрация успешна!"

### Тестирование входа
1. Откройся на `/login`
2. Введи email и пароль которые регистрировал
3. Кликни "Войти"
4. Должен быть редирект на `/` (дашборд)
5. Должны видно твое имя (если ввел при регистрации)

### Тестирование выхода
1. На дашборде, кликни на профиль (верхний правый угол)
2. Кликни "Выход"
3. Должен быть редирект на `/login`
4. Если кликнешь на 'f', должен быть редирект на `/login` (защита работает)

---

## 📊 Проверка console

Открой Developer Tools (F12) -> Console и проверь:

### Что должно быть:
- ✅ Нет красных ошибок типов `Cannot find module`
- ✅ Возможны warning типа "The ?? operator..." (это нормально)
- ✅ При загрузке страницы - нет ошибок Supabase

### Что не должно быть:
- ❌ Uncaught SyntaxError
- ❌ Cannot find module
- ❌ Unexpected token
- ❌ Unhandled promise rejection

---

## 🧬 Hot Module Reload (HMR)

Vite имеет горячую перезагрузку - когда ты меняешь файл, браузер автоматически перезагружается!

### Проверь:
1. Открой браузер на `http://localhost:3001`
2. Открой `src/modules/auth/components/LoginForm.tsx`
3. Измени какой-нибудь текст (например, "Email" на "Почта")
4. Сохрани файл (Ctrl+S)
5. Браузер должен обновиться **автоматически** 🚀

---

## 📱 Адаптивность

### Протестируй на разных размерах экрана

#### Десктоп (1920x1080)
- [ ] Сайдбар видно слева
- [ ] Шапка полноценная
- [ ] Все элементы хорошо расположены

#### Планшет (768x1024)
- [ ] Сайдбар скрывается
- [ ] Кнопка меню в шапке
- [ ] Drawer меню выдвигается

#### Мобильный (375x667)
- [ ] Все элементы под размер экрана
- [ ] Текст читаемый
- [ ] Кнопки кликабельны

**Способ проверки в Chrome:**
1. F12 - Developer Tools
2. Ctrl+Shift+M - Toggle device toolbar
3. Выбери нужный размер (iPhone, iPad, Desktop)

---

## ✅ Финальный чек-лист

Когда закончишь тестирование, отметь:

- [ ] Маршруты работают
- [ ] Форма входа работает
- [ ] Форма регистрации работает
- [ ] Дашборд загружается
- [ ] Навигация работает
- [ ] Адаптивность работает (на мобильном)
- [ ] Нет критических ошибок в console
- [ ] HMR работает (автоматическая перезагрузка)

---

## 🐛 Если что-то не работает

1. **Проверь console** (F12)
   - Есть ли ошибки?
   - Какой текст ошибки?

2. **Проверь .env.local**
   - Заполнены ли переменные?
   - Правильные ли значения?

3. **Перезагрузи сервер**
   ```bash
   # Останови текущий (Ctrl+C)
   # Запусти снова
   npm run dev
   ```

4. **Очистить кэш браузера**
   - F12 -> Application -> Clear storage -> Clear all
   - Или Hard refresh: Ctrl+Shift+R

5. **Проверь GitHub Issues** на наличие похожих проблем

---

## 📞 Получить помощь

Если что-то не работает:

1. Напиши в чем ошибка
2. Скопируй текст из console (F12)
3. Укажи какой браузер используешь
4. Укажи на каком маршруте произошла ошибка

---

**Создано:** 2024-12-04
**Версия:** 0.2.0
**Статус:** ✅ Dev Server Ready

**🚀 Готово тестировать!**
