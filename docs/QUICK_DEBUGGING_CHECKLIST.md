# 🚀 Быстрый Чек-лист Отладки

**При ошибке в pipeline используй этот чек-лист за 5 минут**

---

## Шаг 1: Понять суть ошибки (1 мин)

```
Что вернула система?
  □ 404 Not Found          → неправильный URL
  □ 400 Bad Request        → неправильный формат данных
  □ 500 Server Error       → ошибка в коде функции
  □ Пусто []               → RLS блокирует или нет данных
  □ Timeout / Медленно     → долгая операция (нужен оптимизм)
```

---

## Шаг 2: Посмотри логи (1 мин)

```bash
# Supabase Dashboard → Functions → Logs

Что ищешь:
  ✅ Есть новые логи с эмодзи (🚀 📥 ✅)?
     → Функция развернута и новый код запущен

  ❌ Старые логи без эмодзи?
     → Старый код все еще запущен (переразвертни)

  ❌ Нет логов вообще?
     → Функция не вызвана или ошибка еще раньше
```

---

## Шаг 3: Проверь каждый уровень (3 мин)

### 3.1 Функция развернута?
```bash
npx supabase functions list | grep <function-name>
# Должна быть в списке, VERSION должен быть > 0
```

### 3.2 Правильный URL вызова?
```
✅ /functions/v1/source-hunter
✅ /functions/v1/content-fetcher

❌ /functions/v1/agents/source-hunter  (Supabase не добавляет /agents/)
❌ /functions/v1/my/source-hunter      (не используй пути)
```

### 3.3 Таблица доступна из REST API?
```bash
curl -H "apikey: $ANON_KEY" \
  'https://your-project.supabase.co/rest/v1/table-name?limit=1'

# Результат:
#  ✅ [{ data }]        → доступна
#  ❌ []                → RLS блокирует (смотри 3.4)
#  ❌ 401/403           → auth проблема
```

### 3.4 RLS политика позволяет?
```bash
# Supabase Dashboard → SQL Editor

SELECT polname, poltype FROM pg_policy
WHERE polrelid = 'your_table'::regclass;

# Проверь есть ли для SELECT:
#  ✅ USING (true)                              → можно читать всем
#  ❌ USING (auth.role() = 'authenticated')    → нужна auth (не подойдет для REST)
```

### 3.5 Типы данных соответствуют коду?
```bash
# Supabase Dashboard → SQL Editor

SELECT column_name, data_type FROM information_schema.columns
WHERE table_name = 'your_table'
ORDER BY ordinal_position;

# Проверь:
#  Имена: source_type_id (не type)
#  Типы: UUID, TEXT, INT, etc.

# Потом в коде убедись что SELECT это же:
const { data } = await supabase
  .from('sources')
  .select('id, source_type_id, website_url')  // ТОЧНЫЕ имена
```

---

## Решения для типичных ошибок

### 404: Function not found

```bash
# Причина 1: Неправильный URL
❌ /functions/v1/agents/source-hunter
✅ /functions/v1/source-hunter

# Причина 2: Функция не развернута
npx supabase functions deploy source-hunter
npx supabase functions list | grep source-hunter

# Причина 3: Дублирование функции (две версии)
find supabase/functions -type d -name source-hunter
# Должно быть ОДНО
# Если два - удали один из них
rm -rf supabase/functions/source-hunter  (если есть в root)
```

### 400: Missing parameters или Wrong format

```typescript
// Проверь что ты отправляешь
console.log('Request body:', JSON.stringify(body, null, 2));

// Функция ожидает:
{
  document_ids: ["id1", "id2"],  // ARRAY, не string
  search_run_id: "uuid"
}

// А ты отправляешь:
{
  document_id: "id1",  // SINGLE, не array
  url: "..."
}

// РЕШЕНИЕ: посмотри types.ts функции
// Там описано что ожидается
```

### Пусто []: RLS блокирует

```bash
# Шаг 1: Убедись что данные есть
psql -U postgres -d your_db -c "SELECT COUNT(*) FROM sources;"
# Результат > 0

# Шаг 2: Посмотри RLS политику
SELECT polname FROM pg_policy WHERE polrelid = 'sources'::regclass;

# Шаг 3: Если есть auth-зависимая политика - нужна миграция
# Migration: XXX_fix_rls_for_sources.sql

DROP POLICY IF EXISTS "Sources viewable by users" ON sources;
CREATE POLICY "Sources viewable by all" ON sources
  FOR SELECT USING (true);

# Шаг 4: Примени миграцию
npx supabase db push --include-all --linked
```

### Медленно / Timeout

```bash
# Что делает функция?
# Если обрабатывает 39 документов:
#  - каждый fetch: 1-5 сек
#  - parsing: 100ms
#  - update DB: 100ms
# ВСЕГО: 39 * 5 ~ 195 сек

# Решение:
#  1. Параллельная обработка (Promise.all)
#  2. Оптимизация fetch (меньше retry)
#  3. Батчинг DB queries (обновлять много за раз)
#  4. Timeout увеличить (Supabase: до 10 минут)
```

---

## Разработка новой функции

```
1️⃣  Создай папку: supabase/functions/agents/my-function/
    ├─ index.ts         (основной код)
    ├─ types.ts         (TypeScript типы)
    └─ deno.json        (конфиг)

2️⃣  Напиши код с логированием:
    console.log('🚀 Function started');
    console.log('📥 Input:', input);
    console.log('✅ Success');

3️⃣  Разверни:
    npx supabase functions deploy my-function

4️⃣  Проверь сразу:
    curl https://.../functions/v1/my-function -d '{"test":true}'
    # Смотри в Dashboard → Logs
    # Должны быть твои логи!

5️⃣  Если нет логов:
    # Значит старый код запущен
    # Или функция не развернута
    # Решение: переразвертни
    npx supabase functions delete my-function
    npx supabase functions deploy my-function
```

---

## Интеграция функций

```
1️⃣  Из function A вызвать function B:

    const response = await fetch(
      `${Deno.env.get('SUPABASE_URL')}/functions/v1/function-b`,
      {
        method: 'POST',
        headers: {
          'Content-Type': 'application/json',
          'apikey': Deno.env.get('SUPABASE_ANON_KEY') || '',
          ...(authHeader && { 'Authorization': authHeader }),
        },
        body: JSON.stringify(payload),
      }
    );

2️⃣  Обработай ответ:

    if (!response.ok) {
      const error = await response.text();
      console.error('❌ Function B failed:', response.status, error);
      throw new Error(`Function B failed: ${response.status}`);
    }

    const data = await response.json();
    console.log('📡 Received from B:', data);

3️⃣  Логируй все вызовы:

    console.log('🔍 Calling Function B:', {
      url: functionUrl,
      hasAuth: !!authHeader,
      payload: body
    });
```

---

## После каждого fix

```bash
# 1. Развертни
npx supabase functions deploy source-hunter
npx supabase db push --linked

# 2. Проверь логи СРАЗУ
curl https://.../functions/v1/source-hunter -d '{"test":true}'
# Смотри в Dashboard → Logs
# Есть ли новые логи с эмодзи?

# 3. Проверь данные в БД
curl -H "apikey: $ANON_KEY" \
  'https://.../rest/v1/documents?order=created_at.desc&limit=1'
# Есть ли новые записи?

# 4. Если все OK - коммит
git add -A
git commit -m "fix: описание чего исправил"

# 5. Если не OK - логи покажут почему
# Повторить шаги выше
```

---

## Когда ты застрял (5-минутный план)

```
⏱️  0-1 мин: Сделай вдох. Не паника.

⏱️  1-2 мин: Запиши точный симптом:
             • Что вернула система?
             • На каком шаге произошло?
             • Какие логи есть?

⏱️  2-3 мин: Проверь очевидное:
             • Функция развернута? (npx supabase functions list)
             • URL правильный? (без /agents/)
             • RLS не блокирует? (curl с анон key)

⏱️  3-4 мин: Один minimal fix:
             • Только ОДНО изменение
             • Развертни
             • Проверь логи

⏱️  4-5 мин: Решение или следующий шаг:
             • Помогло? → Коммит и дальше
             • Не помогло? → Вернись к симптому,
                            может быть другая проблема
```

---

## Наиболее частые ошибки (из практики)

```
❌ Error #1: Дублирование функции
   └─ Решение: find . -name index.ts | grep function-name
   └─ Должно быть ровно ОДНО совпадение

❌ Error #2: RLS требует auth для таблицы reference
   └─ Решение: CREATE POLICY ... USING (true)
   └─ Для справочных таблиц - всем чить разрешить

❌ Error #3: Неправильный URL вызова между функциями
   └─ Решение: /functions/v1/{FOLDER_NAME}
   └─ Никогда не используй полный путь

❌ Error #4: Код развернут но логи старые
   └─ Решение: Добавь эмодзи логи
   └─ После развертывания сразу проверь новые логи

❌ Error #5: TypeScript interface не совпадает с реальным ответом
   └─ Решение: Посмотри реальный JSON ответ
   └─ Обнови типы в types.ts
```

---

## Когда вызвать коллегу

```
Вызвать если:
  ✋ Ты застрял > 30 минут на одной проблеме
  ✋ Проблема влияет на весь pipeline
  ✋ Не уверен в своем решении (вторая пара глаз)
  ✋ Нужна помощь с миграцией БД
  ✋ Нужно рефакторить архитектуру

Принести с собой:
  📋 Точное описание ошибки
  📋 Все логи (скопировать из Dashboard)
  📋 Что уже пробовал
  📋 Какие гипотезы есть
```

---

## Полезные команды (сохрани в ~/.bashrc)

```bash
# Проверить состояние functions
alias sf-list='npx supabase functions list'

# Посмотреть логи (требует прокрутки в Dashboard, но можно автоматизировать)
alias sf-logs='echo "Open: Supabase Dashboard → Functions → Logs"'

# Проверить таблицу в БД
sf-count() {
  psql -U postgres -d your_db -c "SELECT COUNT(*) FROM $1"
}

# Проверить RLS на таблице
sf-rls() {
  psql -U postgres -d your_db -c \
    "SELECT polname, poltype FROM pg_policy WHERE polrelid = '$1'::regclass"
}

# Полный тест развертывания
sf-test() {
  echo "1. Checking functions..."
  npx supabase functions list | grep $1
  echo "2. Test function..."
  curl -s -X POST https://your-project.supabase.co/functions/v1/$1 \
    -H "apikey: $ANON_KEY" \
    -H "Content-Type: application/json" \
    -d '{"test":true}' | jq .
}
```

---

**Версия:** 1.0.0
**Дата:** 2025-12-14
**Обновляй этот файл когда найдешь новые ошибки!**
