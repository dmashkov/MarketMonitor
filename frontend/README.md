# MarketMonitor Frontend

Frontend приложения для мониторинга активностей климатического рынка России с использованием AI.

## 🚀 Быстрый старт

### Предварительные требования

- **Node.js**: >= 18.0.0
- **npm**: >= 9.0.0
- **git**: для версионирования

### Установка зависимостей

```bash
# Перейти в папку фронтенда
cd frontend

# Установить зависимости
npm install
```

### Настройка переменных окружения

1. Создайте файл `.env` на основе `.env.example`:

```bash
cp .env.example .env
```

2. Получите учетные данные Supabase:
   - Перейдите на [Supabase Dashboard](https://supabase.com/dashboard)
   - Выберите ваш проект
   - Откройте Settings > API
   - Скопируйте:
     - **Project URL** → `VITE_SUPABASE_URL`
     - **Project API Keys > anon (public)** → `VITE_SUPABASE_ANON_KEY`

3. Заполните `.env`:

```env
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here
```

### Запуск разработчика сервера

```bash
npm run dev
```

Приложение откроется в браузере по адресу `http://localhost:3000`

## 📦 Команды

### Разработка

```bash
# Запустить dev сервер на localhost:3000
npm run dev

# Проверить типы TypeScript
npm run type-check

# Запустить ESLint
npm run lint
```

### Сборка

```bash
# Собрать оптимизированную версию для production
npm run build

# Preview сборки локально
npm run preview
```

## 📁 Структура проекта

```
src/
├── components/           # Переиспользуемые компоненты
│   ├── auth/            # Компоненты аутентификации
│   ├── admin/           # Компоненты администратора
│   ├── shared/          # Общие компоненты
│   └── layout/          # Макеты страниц
│
├── pages/               # Страницы приложения
│   ├── auth/            # Страницы логина и регистрации
│   ├── admin/           # Админ панель
│   └── *.tsx            # Основные страницы
│
├── hooks/               # Custom React hooks
│   ├── useAuth.ts       # Аутентификация
│   ├── useEvents.ts     # Работа с событиями
│   └── ...
│
├── lib/                 # Утилиты и конфигурация
│   ├── supabase.ts      # Supabase клиент
│   ├── types.ts         # TypeScript типы
│   ├── api.ts           # API функции
│   └── ...
│
├── store/               # Глобальное состояние (Zustand)
│
├── App.tsx              # Главный компонент
├── main.tsx             # Entry point
└── index.css            # Глобальные стили (Tailwind)
```

## 🔐 Аутентификация

Приложение использует **Supabase Auth** для управления пользователями:

### Потоки аутентификации

1. **Регистрация**
   - Новый пользователь заполняет форму
   - Supabase создает аккаунт
   - Автоматически создается профиль в БД
   - Первый пользователь получает роль `admin`

2. **Вход**
   - Email + пароль
   - JWT токен сохраняется в localStorage
   - Автоматическое обновление токена

3. **Защищенные маршруты**
   - Компонент `<ProtectedRoute>` проверяет сессию
   - Компонент `<AdminRoute>` проверяет роль

### Использование в компонентах

```typescript
import { useAuth } from './hooks/useAuth';

function MyComponent() {
  const { profile, isAdmin } = useAuth();

  if (isAdmin) {
    return <AdminPanel />;
  }

  return <UserDashboard />;
}
```

## 📊 Технологический стек

### Frontend Framework
- **React 18+** - UI библиотека
- **TypeScript** - типизированный JavaScript
- **Vite** - сборщик и dev сервер

### Состояние и данные
- **Zustand** - управление состоянием
- **TanStack Query** - кеширование и синхронизация данных

### Backend & Authentication
- **Supabase** - PostgreSQL + Auth + Edge Functions
- **@supabase/supabase-js** - SDK для Supabase

### UI & Styling
- **Ant Design** - компоненты UI
- **Tailwind CSS** - утилити CSS
- **Recharts** - графики и диаграммы

### Tools & Quality
- **TypeScript** - статический анализ типов
- **ESLint** - линтинг кода
- **Vite** - быстрая сборка

## 🎯 Основные страницы

### Публичные маршруты
- `/login` - Вход
- `/register` - Регистрация

### Защищенные маршруты (только для авторизованных пользователей)
- `/` - Dashboard (основная страница)
- `/events` - Список событий
- `/reports` - Отчеты и экспорт

### Admin маршруты (только для администраторов)
- `/admin/users` - Управление пользователями
- `/admin/prompts` - Библиотека промптов
- `/admin/scheduler` - Планировщик джобов
- `/admin/search-runner` - Ручной запуск поиска
- `/admin/logs` - Логи выполнения

## 🔌 Интеграция с Supabase

### Инициализация клиента

```typescript
// lib/supabase.ts
import { createClient } from '@supabase/supabase-js';

export const supabase = createClient(
  import.meta.env.VITE_SUPABASE_URL,
  import.meta.env.VITE_SUPABASE_ANON_KEY
);
```

### Использование в компонентах

```typescript
import { supabase } from './lib/supabase';

// Получить данные
const { data, error } = await supabase
  .from('events')
  .select('*')
  .eq('segment', 'кондиционеры');

// Real-time подписка
const subscription = supabase
  .from('events')
  .on('*', (payload) => {
    console.log('Новое событие:', payload.new);
  })
  .subscribe();
```

## 🎨 Кодстайл

### TypeScript
- Используйте явные типы везде где возможно
- Избегайте `any`, используйте `unknown` если нужно
- Используйте интерфейсы вместо `type` для объектов

### React компоненты
- Функциональные компоненты с hooks
- Кастомные hooks для переиспользуемой логики
- JSX с TypeScript

### CSS/Tailwind
- Используйте Tailwind классы
- Избегайте inline стилей
- Используйте компоненты Ant Design где возможно

## 📝 Примеры

### Создание защищенного компонента

```typescript
import { useAuth } from './hooks/useAuth';

function AdminPanel() {
  const { profile, isAdmin } = useAuth();

  if (!isAdmin) {
    return <div>Access Denied</div>;
  }

  return <div>Admin Content</div>;
}
```

### Использование TanStack Query

```typescript
import { useQuery } from '@tanstack/react-query';
import { supabase } from './lib/supabase';

function Events() {
  const { data: events, isLoading } = useQuery({
    queryKey: ['events'],
    queryFn: async () => {
      const { data } = await supabase
        .from('events')
        .select('*');
      return data;
    },
  });

  if (isLoading) return <div>Loading...</div>;

  return <EventsList events={events} />;
}
```

### Работа с Zustand

```typescript
import { create } from 'zustand';

const useAppStore = create((set) => ({
  user: null,
  setUser: (user) => set({ user }),
}));

function MyComponent() {
  const { user } = useAppStore();
  return <div>{user?.email}</div>;
}
```

## 🐛 Troubleshooting

### Ошибка: "Missing VITE_SUPABASE_URL"
- Проверьте, что файл `.env` существует
- Убедитесь, что переменные заполнены корректно
- Перезагрузите dev сервер: `npm run dev`

### Ошибка: "Supabase connection test failed"
- Проверьте URL Supabase в `.env`
- Убедитесь, что вы в интернете
- Проверьте, что ключи API корректны

### Медленная загрузка приложения
- Очистите node_modules: `rm -rf node_modules && npm install`
- Очистите кеш Vite: `npm run build -- --force`
- Проверьте размер бандла: `npm run build -- --report`

## 📚 Документация

- [Supabase документация](https://supabase.com/docs)
- [React документация](https://react.dev)
- [Vite документация](https://vitejs.dev)
- [Tailwind CSS документация](https://tailwindcss.com/docs)
- [Ant Design документация](https://ant.design)

## 📄 Лицензия

MIT - смотри LICENSE файл

## 🤝 Разработка

Для развития проекта смотрите [DEVELOPMENT.md](../docs/development.md)

## 📧 Контакты

Для вопросов и предложений откройте Issue на GitHub.
