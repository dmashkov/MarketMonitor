# CLAUDE.md - Контекст разработки MarketMonitor

> Этот файл автоматически загружается в контекст каждой сессии Claude Code
>
> **Версия:** 1.2.0 (2024-12-13)
> **Проект:** MarketMonitor v0.6.0
> **Статус:** ✅ Phase 3 Complete (Admin UI 100%), 🚀 Phase 4 Starting (AI Agents Implementation)
> **Новая архитектура:** Multi-Agent System (см. AI_AGENTS_ARCHITECTURE.md)

---

## 🤖 ВАЖНО ДЛЯ AI-АГЕНТА

### При первом сообщении в новой сессии ОБЯЗАТЕЛЬНО:

1. ✅ **Подтверди загрузку контекста:**
   ```
   ✅ CLAUDE.md загружен в контекст
   📂 Проект: MarketMonitor v0.5.0
   🚀 Текущая фаза: Phase 3 - AI Agents Architecture
   ```

2. ✅ **Проактивно прочитай:**
   - `DEVELOPMENT_STATUS.md` - текущий прогресс (2 минуты)
   - `AI_AGENTS_ARCHITECTURE.md` - новая архитектура агентов (5 минут)
   - `TODO.md` - список задач (если разрабатываешь конкретное)

3. ✅ **Краткий анализ:**
   - Текущая версия и статус
   - Что в работе (из DEVELOPMENT_STATUS.md)
   - Есть ли блокеры

4. ✅ **Скажи явно:**
   ```
   ⏸️ Готов работать! Что делаем?
   ```

5. ⏳ **ЖДАТЬ ответа пользователя!**

**НЕ жди пока пользователь попросит - будь ПРОАКТИВНЫМ!**

---

## 📋 Что это за проект?

**MarketMonitor** - AI-powered приложение для автоматизированного мониторинга климатического рынка России.

### Основные характеристики:
- **Целевая аудитория:** Руководители, менеджеры, маркетологи
- **Ключевая функция:** Автоматический поиск рыночных событий через OpenAI API + AI Agents
- **MVP:** 6 недель разработки (5 фаз)
- **Версия:** 0.6.0 (Phase 3 Complete + Phase 4 Starting)
- **Статус:** ✅ Phase 3 Complete (Admin UI 100%), 🚀 Phase 4 Starting

---

## 🤖 НОВАЯ АРХИТЕКТУРА: AI Agents 2.0

### Парадигмальный сдвиг

**БЫЛО (неправильно):**
- Daily/Weekly/Monthly промпты ищут данные каждый раз
- Нет накопления истории
- Повторения и дубликаты

**СТАЛО (правильно):**
```
Daily Search (сбор) → Documents DB (хранение) → Reports (анализ)
```

### Multi-Agent Pipeline (8 агентов)

```
Source Hunter → Content Fetcher → Document Processor →
→ Event Extractor → Criticality Scorer → Duplicate Detector →
→ Save to DB → Alert Manager
```

**Каждый агент решает ОДНУ задачу!**

### Новые таблицы (Migration 007)

1. **brands** - справочник брендов (Daikin, Midea, Haier, Ballu, etc.)
2. **documents** - полное хранение контента:
   - PDF/PPTX → Supabase Storage
   - HTML → content_html
   - Текст → content_text (FTS поиск)
   - **Embeddings** → vector(1536) для семантического поиска
3. **reports** - сохраненные отчеты (daily/weekly/monthly)
4. **custom_prompts** - кастомные запросы пользователей

**См. полное описание:** [AI_AGENTS_ARCHITECTURE.md](AI_AGENTS_ARCHITECTURE.md)

---

## 🎯 КЛЮЧЕВЫЕ ТРЕБОВАНИЯ РАЗРАБОТКИ

### 1. Модульная Архитектура (НЕ монолитная!)

**Структура модулей:**
```
frontend/src/modules/
├── auth/              # Аутентификация
├── admin/             # Admin панель
│   ├── users/
│   ├── prompts/
│   ├── brands/        # 🆕 Управление брендами
│   ├── sources/       # Управление источниками
│   └── documents/     # 🆕 Библиотека документов
├── events/            # Просмотр событий
├── analytics/         # Дашборды
└── export/            # Экспорт отчетов

shared/                # Переиспользуемый код
lib/                   # Библиотеки (supabase, openai)
```

**Правила изоляции:**
- ✅ Модуль импортирует из `shared/` и `lib/`
- ✅ Модуль импортирует из собственной папки
- ❌ Модуль НЕ импортирует из другого модуля напрямую

**Пример OK:**
```typescript
// modules/events/hooks/useEvents.ts
import { MarketEvent } from '@/shared/types';  // ✅ OK
import { supabase } from '@/lib/supabase';      // ✅ OK
```

**Пример ЗАПРЕЩЕНО:**
```typescript
// modules/events/components/EventList.tsx
import { AdminPanel } from '@/modules/admin';   // ❌ NO!
```

### 2. Строгая Типизация (NO ANY!)

**Запрещено:**
```typescript
❌ const data: any = ...
❌ function process(value: any) {}
❌ (data as any).property
```

**Обязательно:**
```typescript
✅ interface UserData { id: string; name: string; }
✅ const data: UserData = ...
✅ function process(value: string): void {}
```

**Везде:**
- ✅ Функции: явные параметры и return типы
- ✅ API ответы: типизированы через interface
- ✅ React Query hooks: типизированные
- ✅ Form данные: типизированные через react-hook-form + zod

### 3. OpenAI API (НЕ Claude!)

**Используй:**
- ✅ `import OpenAI from "openai"`
- ✅ `OPENAI_API_KEY` переменная окружения
- ✅ Модели:
  - `gpt-4o` - для extraction, scoring, reports
  - `gpt-4o-mini` - для дешевых операций (mentions extraction)
  - `text-embedding-3-small` - для embeddings (1536 dimensions)

**НЕ используй:**
- ❌ `import Anthropic from "@anthropic-ai/sdk"`
- ❌ `ANTHROPIC_API_KEY`
- ❌ Claude модели

### 4. Netlify Deploy

**Требования:**
- ✅ `.env.local` для локальных переменных
- ✅ `.env` в `.gitignore` (никогда не коммитить!)
- ✅ `npm run build` работает
- ✅ Оптимизирован для SPA (React Router)
- ✅ Все бизнес-логика в Edge Functions на Supabase

### 5. Supabase Storage (NEW!)

**Bucket:** `market-documents`

**Структура:**
```
market-documents/
├─ pdfs/2024/12/
├─ presentations/2024/12/
└─ user-uploads/{user_id}/
```

**Политики:**
- Authenticated users: READ
- Admins: READ, WRITE, DELETE
- Users: WRITE только в user-uploads/{user_id}/

### 6. pgvector для Semantic Search (NEW!)

**Требуется:**
- Extension: `vector` включен в Supabase
- Index: `ivfflat` для column `embedding`
- Размерность: 1536 (OpenAI text-embedding-3-small)

**Поиск:**
```sql
SELECT id, title, 1 - (embedding <=> $1::vector) as similarity
FROM documents
WHERE 1 - (embedding <=> $1::vector) > 0.7
ORDER BY embedding <=> $1::vector
LIMIT 10;
```

---

## 🚀 Структура Проекта

```
MarketMonitor/
├── frontend/                      # React приложение (Netlify)
│   ├── src/
│   │   ├── modules/              # 5 модулей + новые admin модули
│   │   ├── shared/               # Переиспользуемый код
│   │   ├── lib/                  # Supabase, OpenAI клиенты
│   │   ├── pages/                # Страницы (маршруты)
│   │   └── store/                # Zustand состояние
│   ├── package.json              # 25+ зависимостей
│   └── .env.local                # Переменные (в .gitignore!)
│
├── supabase/                      # Backend
│   ├── migrations/
│   │   ├── 001-004_*.sql         # ✅ Применены
│   │   ├── 005_sources_and_segments.sql  # ✅ Готова
│   │   ├── 006_seed_sources.sql  # ✅ Готова
│   │   └── 007_brands_and_documents.sql  # ⏳ TODO
│   ├── functions/
│   │   ├── sources-api/          # ⏳ В разработке
│   │   ├── brands-api/           # ⏳ TODO (новое)
│   │   ├── documents-api/        # ⏳ TODO (новое)
│   │   └── agents/               # ⏳ TODO (8 агентов)
│   │       ├── source-hunter/
│   │       ├── content-fetcher/
│   │       ├── document-processor/
│   │       ├── event-extractor/
│   │       ├── criticality-scorer/
│   │       └── duplicate-detector/
│
├── docs/
│   ├── architecture.md           # Полная архитектура
│   └── progress.md               # Отслеживание
│
├── CLAUDE.md                     # 👈 ЭТОТ ФАЙЛ - AI контекст
├── DEVELOPMENT_STATUS.md         # 📊 Текущий статус
├── TODO.md                       # ✅ Список задач
├── AI_AGENTS_ARCHITECTURE.md     # 🤖 Архитектура агентов (новое!)
├── ROADMAP.md                    # 🗺️ Долгосрочный план
├── README.md                     # Главный README
└── SESSION_CHECKPOINT_2024-12-07.md  # Последний снапшот
```

---

## 📚 Ключевые Файлы для AI

### При разработке ВСЕГДА читай в таком порядке:

1. **DEVELOPMENT_STATUS.md** (2 минуты)
   - Текущая фаза разработки
   - Что завершено
   - Что дальше

2. **AI_AGENTS_ARCHITECTURE.md** (5 минут, если работаешь с агентами)
   - Полная архитектура 8 агентов
   - Схема базы данных для новых таблиц
   - План реализации

3. **TODO.md** (2 минуты, если разрабатываешь конкретное)
   - Список задач с приоритетами
   - Что именно делать дальше

4. **docs/architecture.md** (10 минут, если нужны детали)
   - Полная архитектура всего приложения
   - Tech stack
   - API endpoints
   - Database schema

---

## ⚙️ Tech Stack

### Frontend (Netlify)
- **React 18** + **TypeScript** (strict mode, NO ANY!)
- **Vite 5** - сборщик
- **React Router 6** - маршруты
- **TanStack Query 5** - кэширование данных
- **Zustand** - состояние
- **Tailwind CSS** + **Ant Design** - стили
- **Recharts** - графики
- **zod** - валидация типов

### Backend (Supabase)
- **PostgreSQL** - БД
- **Supabase Auth** - аутентификация
- **Row Level Security** - безопасность
- **Edge Functions** - бизнес-логика
- **Supabase Storage** - файлы (PDF, PPTX) 🆕
- **pgvector** - семантический поиск 🆕

### AI Layer
- **OpenAI API** (НЕ Claude!)
  - `gpt-4o` - основная модель
  - `gpt-4o-mini` - дешевая модель
  - `text-embedding-3-small` - embeddings
- Type-safe responses (interfaces)

### DevOps
- **GitHub** - версионирование
- **Netlify** - Frontend хостинг (SPA)
- **Supabase** - Backend хостинг

---

## 🎯 Фазы Разработки

| Фаза | Статус | Описание | Длительность |
|------|--------|----------|--------------|
| **Phase 1** | ✅ DONE | Foundation & Docs | 1 неделя |
| **Phase 2** | ✅ DONE | Auth & Events MVP | 1 неделя |
| **Phase 3** | ✅ DONE | Admin UI Complete | 2 недели |
| **Phase 4** | 🚀 NOW | AI Agents Implementation | 3-4 недели |
| **Phase 5** | 📋 NEXT | Production Ready + Cron | 1-2 недели |

**Текущая:** Phase 4 (0% - Starting Source Hunter Agent)

---

## 🔧 Bash команды для работы

### Git команды:
```bash
git status                    # Проверить статус
git diff                      # Посмотреть изменения
git log --oneline -n 10       # Последние 10 коммитов
```

### Frontend:
```bash
cd frontend
npm install              # Установить зависимости
npm run dev             # Запустить dev сервер
npm run build           # Build для Netlify
npm run type-check      # Проверить TypeScript
npm run lint            # Lint код
```

### Supabase (когда будет установлен):
```bash
npx supabase status          # Статус локального Supabase
npx supabase db push         # Применить миграции
npx supabase functions serve # Запустить Edge Functions локально
```

---

## ⚠️ Предупреждения для AI

**НИКОГДА НЕ:**
- ❌ Добавлять код с `any`, `unknown`, `as any`
- ❌ Создавать новые модули без обсуждения
- ❌ Импортировать между модулями напрямую
- ❌ Использовать Claude AI (только OpenAI!)
- ❌ Коммитить `.env` файлы

**ВСЕГДА:**
- ✅ Проверяй модульность
- ✅ Типизируй код
- ✅ Используй OpenAI API
- ✅ Помни про Netlify требования
- ✅ Читай DEVELOPMENT_STATUS.md перед началом работы

---

## 💡 Что делать если...

**Вопрос: Как разработать фичу X?**
➜ Прочитай TODO.md, найди задачу, следуй чек-листу

**Вопрос: Как типизировать Y?**
➜ Посмотри примеры в src/lib/types.ts или shared/types.ts

**Вопрос: Где должен быть компонент Z?**
➜ Определи модуль: if auth → modules/auth/components, if events → modules/events/components, if общий → shared/components

**Вопрос: Как деплоить?**
➜ Frontend: `npm run build` → deploy на Netlify
➜ Backend: миграции через Supabase Dashboard, Edge Functions через `supabase functions deploy`

**Вопрос: Как создать миграцию?**
➜ См. примеры в `supabase/migrations/`, следуй нумерации 001-007

---

## 📞 Вопросы?

1. 📖 Смотри [DEVELOPMENT_STATUS.md](DEVELOPMENT_STATUS.md) - текущий статус
2. 📋 Проверь [TODO.md](TODO.md) - список задач
3. 🤖 Изучи [AI_AGENTS_ARCHITECTURE.md](AI_AGENTS_ARCHITECTURE.md) - новая архитектура
4. 💻 Используй TypeScript - IDE подскажет
5. 🚀 Следуй модульной архитектуре

---

**Версия документа:** 1.2.0 (2025-12-13)
**Последнее обновление:** Phase 3 Complete, Phase 4 Starting
**Следующее обновление:** Автоматически при каждом git push
